# 2026-09-08 验收记录

## 范围

独立首页、Get Started、注册/登录、项目会话目录、底部账户、默认/自定义模型、黑白主题、PDF证据批注、岗位资格过滤和PDF/DOCX优化稿下载。

## 已验证

- 完整 npm test 通过；后续修改重新运行对应 agent、app、adapter、UI 测试。生产构建通过。
- 浏览器桌面亮/暗三栏，长对话独立滚动；390×844 窄屏对话、固定输入框和右侧抽屉无横向溢出。修复了 grid 自动最小高度和隐藏标题占行问题。
- 隔离账户上传中文 PDF，真实模型分析显示原文批注，原 PDF 预览能显示高亮。
- 真实模型根据用户 JD 生成优化稿。认证下载 PDF 为一页、中文可提取；DOCX 为可编辑文档。批注 PDF 实际包含3个高亮区域。
- 浏览器项目重命名保存、自定义模型字段切换、首页注册入口验证。默认模型不显示供应商品牌。
- 默认与自定义 Anthropic 真实请求通过；OpenAI 协议通过确定性适配测试，未使用真实 OpenAI 密钥做调用。
- 资格回归包含本科/PhD、专业、经验、签证、技能完整词匹配、过期岗位、聚合信息不足和偏好条件。聚合抓取时间不作为岗位新鲜度证据。
- 密钥仅在服务端，存储与文档均校验用户所有权；现有 package-lock.json 三行差异未改动。

## 边界

聚合来源通常不含完整招聘要求，因此保守列入待核实；不能据此承诺岗位仍开放。建议和优化稿基于用户提供的事实，仍需用户确认个人资料。PDF高亮仅对能匹配原文的文本生效；扫描图片PDF不承诺OCR定位。导出运行依赖已配置的Python文档库，安装方式见README。

测试材料为隔离账户的虚构样例；未修改真实用户账户和简历。

补充：批注支持顶层JSON数组、格式修复、原文引用ID及PDF空白字符差异匹配。项目/实习证据检查不会因模型只输出表面建议而缺失，规则补充明确标注。新增回归均通过。

后台知识服务验收：完整测试与生产构建通过。两个来源真实同步成功（job-radar 3768条、campus-radar 1199条源记录），仅导入招聘字段；测试覆盖失败保留、URL去重、源时间不续期、归档保留、用户数据隔离、重启到期检查和租约互斥。服务已启用定时同步；前台未新增运维入口。接口双通道读取均受超时和大小限制。
