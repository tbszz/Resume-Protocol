# 聊天可靠性优化

范围：保留纯聊天界面和现有依赖，修复缓存恢复、接口错误处理和中文输入问题。

1. 先运行现有测试建立基线，再为损坏缓存、非 JSON 响应、请求超时和输入法回车补充回归测试。
2. 恢复缓存时补齐旧字段并过滤无效数据，保存失败时向用户显示明确提示。
3. 为接口请求设置超时，保留服务器业务错误，并将网络/解析异常转换为可理解的信息。
4. 输入法组合期间不发送消息，保留 Enter 发送和 Shift+Enter 换行。
5. 运行全部测试、生产构建和差异检查。既有 package-lock.json 修改保持原状。
