# 2026-09-07 实现验收

## 已完成

- 按此前最终视频首页提示词恢复指定视频、白色导航、衬线标题、深色底部输入框和单一聊天入口。
- SQLite 持久化账户、会话和资料；原始附件保存在 data/uploads，所有访问按登录用户隔离。
- MiniMax 实际 API、多轮 tool_use/tool_result、诊断/真实岗位检索/JD选择/岗位版简历/面试计划。
- 上传、原文件下载、会话导出、完整简历复制和打印样式。
- 服务端密钥、密码 scrypt、会话令牌哈希、HttpOnly cookie、Origin 检查和并发保护。

## 验证证据

- 全量 npm test 通过，包括两用户隔离、数据库重开、SSE中断、真实协议帧、上传/导出/删除、模型失败和审校空字段回归。
- npm run build 与 git diff --check 通过。
- 独立代码复核未发现剩余高影响阻碍；并发 smoke：运行中的会话删除和第二条消息均返回409，首条消息正常完成。
- 实际 MiniMax M2.7 完成分析、JD选择、简历生成与七天面试计划；并非仅用 mock 验证。
- 实际公开岗位来源请求成功，返回真实来源链接。
- 浏览器本地验收账户完成注册、分析、生成、面试、服务重启后的登录与聊天恢复、TXT上传、Markdown下载、原文件下载、注销。
- 浏览器390px视口 DOM宽度390px，无页面横向溢出；输入框与导出栏无重叠。视口覆盖已恢复。
- 浏览器错误日志为空；.env和data被Git忽略；src/dist未包含配置的API密钥。

## 边界

当前为本机服务，未发布公网。没有邮箱验证/邮件找回密码、扫描PDF OCR或自动投递。规则评分仅作参考；模型输出虽经事实审校和结构校验，仍需要用户投递前核对。视频由此前指定外部地址加载，加载失败保留背景与操作入口。

使用说明与部署环境变量见 README.md；验收只使用虚构材料。
