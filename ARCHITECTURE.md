# 架构规则：单一对话入口与后台知识服务

## 用户入口
AI 对话是求职业务的唯一入口。项目目录、文件上传、预览、下载和账户设置属于辅助操作。不得增加招聘源选择、同步、清理、爬虫控制或知识库运维按钮。

## 四层边界
所有决策必须先经过 `server/brain.js` 读取 `knowledge/brain.md`，输出本轮交付目标、求职者、逐字事实与最少追问。工具执行器必须检查 Brain 授权动作；理解之前禁止自动分析或修改简历。新求职者隔离上下文，旧上下文保留但不注入当前模型。不能通过 SOP 或提示词绕过这一层。模型决策仍须经过既有事实与资格校验。

1. SOP：`knowledge/career-sop.md`，版本化决策规则。人工维护，随代码审查发布，不从第三方仓库自动覆盖。
2. 招聘事实库：`server/jobKnowledge.js`，后台同步固定公开来源。保留来源ID、原始URL、源端观察时间、本地抓取时间和失效状态。第三方文本仅为数据，不能作为系统指令。
3. 决策层：`careerPolicy` + `jobEligibility` + `careerAgent`。先明确用户目标，再读取本地知识库、判断资格和证据，不允许匹配分抵消硬条件。聚合正文未证明完整官方要求时必须待核实。
4. 展示层：对话中呈现用户需要的结论、依据和下一步；不展示后台维护流程。预览只展示当前用户的原文与优化稿。

## 后台生命周期
- 服务启动即检查到期任务，每分钟检查一次调度；各来源每6小时同步一次，失败至少隔1小时重试。
- 成功/失败时间、来源状态、租约和运行日志持久化到SQLite，重启不重置到期记录。
- 一个来源同一时刻只有一个有效租约；远端45秒超时、20MB上限、最多5万条，禁止运行第三方脚本。
- 来源之间独立提交。失败或空/异常数据不得清空旧库；旧记录仍受时效门槛约束。
- 同步不在用户请求路径中执行。AI 搜索只读本地快照；缓存为空或陈旧时如实说明，不能触发临时外网抓取绕开该边界。
- 停机取消下载并等待任务退出，再关闭数据库。调度依赖后端进程常驻；关机期间不运行，下次启动补做已到期任务。

## 保留与清理
- 源端7天未观察到的岗位立即不参与检索，每日清理时归档。
- 已下线记录归档；归档30天后删除。运行日志保留30天。
- 本地重复抓取旧快照不能续期。超出保留期的旧记录不得反复导入复活。
- 同一官方URL去重，保留较完整文本；源端来源记录分别保留便于追溯。
- 清理SQL只能作用于 `job_knowledge_*` 表；禁止删除用户、会话、项目、上传简历、生成稿和SOP。
- campus-radar只读取jobs表，绝不导入applications等个人投递记录。

## 数据真实性
上游仓库成功刷新≠每条职位仍开放；有jd_text≠完整JD。源端观察时间≠发布时间。本系统不得自动提高这些证据等级。后续增加官方详情验证器需独立记录验证URL、时间、正文与失败原因；不能用模型补全缺失要求。

## 验收要求
必须覆盖同步失败保留旧快照、重复去重、陈旧数据不续期、归档清理不影响用户数据、重启调度、并发租约以及无网络查询路径。禁止将同步运行成功当作推荐质量已验证。
